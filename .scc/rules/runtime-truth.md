---
description: Keep runtime truth authoritative across backend, CLI, and web.
paths: backend/src, frontend/src, scripts
---
If a task, provider, approval, archive state, or artifact state is displayed differently across surfaces, fix the truth path first and the presentation second.

Do not accept a UI-only workaround when the backend source of truth is wrong or ambiguous.
