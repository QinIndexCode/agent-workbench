---
description: Inspect saved vs runtime provider truth and explain mismatches.
args: [providerId]
when: Use when Connections, CLI provider status, or runtime provider behavior looks inconsistent.
---
Inspect provider truth for ${args}.

Focus on:

1. saved default provider
2. runtime default provider
3. restart or reload requirements
4. whether the UI or CLI is masking a backend truth issue

Use backend evidence first, then explain the user-facing impact.
